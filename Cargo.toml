[package]
name = "component-crypto"
description = "Cryptographic hashing, HMAC, and JWT decoding"
version = "0.2.0"
edition = "2024"

[dependencies]
act-sdk = "0.3"
wit-bindgen = { version = "0.56", features = ["async-spawn"] }
sha2 = "0.10"
sha3 = "0.10"
hmac = "0.12"
hex = "0.4"
serde_json = "1"
base64 = "0.22"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
opt-level = "z"

[lib]
crate-type = ["cdylib"]
